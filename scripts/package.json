{
  "name": "family-gallery-ingest",
  "private": true,
  "type": "module",
  "description": "Local ingest for the /family gallery — never deployed. Requires ffmpeg/ffprobe (brew install ffmpeg) and macOS sips for HEIC.",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "exiftool-vendored": "^28.0.0",
    "sharp": "^0.33.0"
  }
}
